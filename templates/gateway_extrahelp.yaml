{{- if .Values.gateway.extrahelp }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "osrd.fullname" . }}-gateway-extrahelp
data:
  help.md: |
    {{ .Values.gateway.extrahelp | nindent 4 }}
{{- end }}
